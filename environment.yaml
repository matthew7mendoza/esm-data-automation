name: esm_llm_env
channels:
  - conda-forge
  - noaa-gfdl
dependencies:
  - python=3.12
  - jupyter
  - ffmpeg
  - pip
  - cmor>=3.15.0
  - netcdf4>=1.7
  - numpy>=2
  - pip:
      - -e .
      - mypy
      - pytest
      - ruff